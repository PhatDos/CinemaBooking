# CinemaBooking

Modular monolith cinema booking API built with ASP.NET Core, Entity Framework Core, SQL Server, Redis seat holds, JWT authentication, and Swagger.

## Local Configuration

Keep real secrets out of git. Use `src/CinemaBooking.Api/appsettings.Development.json`, user secrets, or environment variables for local SQL Server, Redis, and JWT values.
